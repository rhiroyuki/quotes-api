### Quotes API

## Requirements
- NodeJS 10.15
- MongoDB 4.0

## Setup

- Copy `.env.sample` to `.env` in the root directory of your project
- Run `$ npm install`
- Run `$ npm run dev`

Or

- Run the script `scripts/setup.sh`
